# Examples

## Thermostat CLI

List all thermostats:
```bash
go run ./thermostat -user=admin -pass=secret
```

Show details for a specific thermostat:
```bash
go run ./thermostat -user=admin -pass=secret -uid=12345
```
